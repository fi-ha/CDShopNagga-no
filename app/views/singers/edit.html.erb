<h2>アーティスト名編集</h2>
<%= form_for(@singer) do |f| %>
     <h4>アーティスト名<%= f.text_field :singer_name, :size => '50' %></h4>
     	<%= f.submit "編集", class: "btn btn bg-success"%>
	 <% end %>
   <%= f.text_field :singer_name, :size => '50' %>
   <%= f.submit "編集", class: "edit_#{@singer.id} btn btn bg-success"%>
<% end %>
