//この処理はindex.html.erbの株で行っている
$(".admin_selecter").on('ajax:success', function() {
  // const innertext = $(this).text();
  // if (innertext == "管理者にする") {
  //   $(this).text("ユーザーにする");
  // } else {
  //   $(this).text("管理者にする");
  // }
});
