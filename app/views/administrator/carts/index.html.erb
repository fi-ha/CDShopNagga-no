<div class='container'>
    <div class='row'>
<!---左のタブの部分テンプレート--->
<%= render 'administrator/tab' %>


<div class="col-lg-7">
    <h2>未発送一覧</h2>
        <% @carts.each do |cart| %>
        <% if cart.status == '未発送' %>
        <div class="", style="padding: 10px; margin-bottom: 10px; border: 1px solid #333333; border-radius: 10px;">
          <p><strong>
          <%= cart.created_at.strftime('%Y/%m/%d') %></strong>の注文
          <strong>注文番号</strong>：
          <%= cart.id %>
		      <strong>ユーザー名</strong>：
          <%= cart.user.last_name %><%= cart.user.first_name %>
          <strong>注文金額</strong>：
          <%= cart.total_price %>円
          <strong><%= cart.status %></strong>
          <%# <%= link_to @cart.status, cart_toggle_status_path(@cart), method: :patch %>
          </p>
<!-------- カートの中身 --------------->
    <div>
      <%# <% cart.items.each do |item|%>
      <%# <%= attachment_image_tag(item, :image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg") %>
		<%# <%end%>商品画像
    </div>

    <div>
    <%# <% cart.items.each do |item|%>
    商品名：ああああああ
    <%# <%= item.item_name %>
		<%# <%end%>
    </div>

    <div>
    <%# <% cart.item_carts.each do |item_cart|%>
    購入枚数：100
    <%# <%= item_cart.count %>
		<%# <%end%>
    </div>

    <div>
    <%# <% cart.items.each do |item|%>
    価格：1000円
    <%# <%= item.price %>
		<%# <%end%>
    </div>


    <div>
    <%# <% cart.ship_to_anothers.each do |ship_to_another|%>
    送付先：
    <%# <%= ship_to_another.first_name %>
    <%# <%= ship_to_another.last_name %>
    <%# <%= ship_to_another.postal_code %>
    <%# <%= ship_to_another.address %>
		<%# <%end%>
    〒<%= cart.user.postal_code %><br>
    <%= cart.user.addres %>
    </div>


</div>
<% end %>
<% end %>


<h2>発送済一覧</h2>
        <% @carts.each do |cart| %>
        <% if cart.status == '発送済み' %>
        <div class="", style="padding: 10px; margin-bottom: 10px; border: 1px solid #333333; border-radius: 10px;">
          <p><strong>
          <%= cart.created_at.strftime('%Y/%m/%d') %></strong>の注文
          <strong>注文番号</strong>：
          <%= cart.id %>
		      <strong>ユーザー名</strong>：
          <%= cart.user.last_name %><%= cart.user.first_name %>
          <strong>注文金額</strong>：
          <%= cart.total_price %>円
          <strong><%= cart.status %></strong>
          <%# <%= link_to @cart.status, cart_toggle_status_path(@cart), method: :patch %>
          </p>
<!-------- カートの中身 --------------->
    <div>
      <%# <% cart.items.each do |item|%>
      <%# <%= attachment_image_tag(item, :image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg") %>
		<%# <%end%>商品画像
    </div>

    <div>
    <%# <% cart.items.each do |item|%>
    商品名：ああああああ
    <%# <%= item.item_name %>
		<%# <%end%>
    </div>

    <div>
    <%# <% cart.item_carts.each do |item_cart|%>
    購入枚数：100
    <%# <%= item_cart.count %>
		<%# <%end%>
    </div>

    <div>
    <%# <% cart.items.each do |item|%>
    価格：1000円
    <%# <%= item.price %>
		<%# <%end%>
    </div>


    <div>
    <%# <% cart.ship_to_anothers.each do |ship_to_another|%>
    送付先：
    <%# <%= ship_to_another.first_name %>
    <%# <%= ship_to_another.last_name %>
    <%# <%= ship_to_another.postal_code %>
    <%# <%= ship_to_another.address %>
		<%# <%end%>
    〒<%= cart.user.postal_code %><br>
    <%= cart.user.addres %>
    </div>


</div>
<% end %>
<% end %>


</div>
    </div>
</div>